# 情绪手账 · 综合改进建议书

> **核心目标**：在综合设计评审中以「创新性 · 完整性 · 情感化 · 技术深度」四个维度拿到高分。

---

## 一、现状评估（已经做得很棒的部分）

经过仔细阅读全部 11 个页面、5 个工具模块和 1 个云函数，你的项目已经具备了扎实的功能基础：

| 模块 | 完成度 | 亮点 |
|---|---|---|
| 首页天气+心情联动 | ★★★★★ | 动态天气背景动画、真实+模拟双通道、上下文问题 |
| 心情记录 | ★★★★★ | 6 项进度引导、9 张图、60s 录音、多维标签 |
| 情绪日历 | ★★★★★ | 热力图、色带、月度对比、最佳/低谷日 |
| 晴雨表分析 | ★★★★★ | EHI 公式拆解、温度×心情、天气分组 |
| 天气心情地图 | ★★★★☆ | 个性化洞察、样本量门槛（"再多记录几次"）|
| 周报 | ★★★★★ | 10 模块分析、温柔总结、与上周对比 |
| 我的 | ★★★★☆ | 分享卡 Canvas 2D、行楷字体加载 |

**视觉系统**已经非常成熟：暖色调（#FFF8F2 → #E8856A）、28rpx 圆角卡片、玻璃拟态、EHI 配色都到位。

---

## 二、隐患与待优化项（P0 · 立即修复）

### 1. 云同步失败无感知
**位置**：`utils/store.js` 的 `saveRecord()`

```js
// 当前实现：catch 后静默
}).catch(err => {
  console.warn('[Cloud] 上传失败:', err);
  // ❌ 用户完全不知道云端没同步上
})
```

**风险**：本地有数据但云端丢失，换设备就丢记录。

**修复建议**：
- 增加同步状态字段（`record.cloudSynced`）
- 增加「云同步失败，点击重试」提示条
- 增加「数据备份」入口（导出 JSON）

### 2. 数据导出与备份缺失
当前没有任何数据导出能力，建议增加：
- `pages/data-export/data-export` 页面
- 支持导出 JSON / Markdown 日记 / PDF 周报
- 一键导入（防止误删）

### 3. 硬编码的地图 Key
**位置**：`cloudfunctions/getWeather/index.js:13`
```js
const TENCENT_MAP_KEY = 'I7DBZ-AQNC3-...';
```

**风险**：开源后 Key 会泄漏，配额耗尽。

**修复建议**：移到云函数环境变量 `process.env.TENCENT_MAP_KEY`。

### 4. `project.config.json` 的 `enhance: false`
这是上次的"白屏事故"根因。**强烈建议改为 `true`**：

```json
{
  "setting": {
    "enhance": true,    // ← 改这里
    "es6": true,
    "postcss": true
  }
}
```

`enhance: true` 后可以用：
- async/await（不用 `.then().catch()`）
- CSS 变量（`--primary`）
- npm 包
- 自定义组件
- 这些都是综合设计评审的高分项

### 5. 无首次使用引导（Onboarding）
新用户进入直接看到一堆功能，会不知所措。建议增加：
- 3 步滑屏引导（图文）
- 第一次记录时的"小助手"动画
- 关键功能的"?" 浮动提示

### 6. 错误边界缺失
任何一个页面 JS 报错都可能导致整个 TabBar 切换异常。建议：
- 在每个 `onLoad` 包 try-catch
- 增加全局错误捕获 `App.onError`
- 增加降级 UI（"数据加载失败，下拉重试"）

---

## 三、核心体验增强（P1 · 本周上线）

### 7. 情绪树成长系统 🌱（最具创新点的功能）
**为什么是它**：综合设计评审最容易拿高分的就是"有情感化隐喻"的视觉系统。

**核心概念**：用户每次记录心情，树上就会长出一颗"心情果实"，颜色对应情绪分值。

**实现思路**：
- 一棵固定的 SVG/CSS 树（程序化绘制）
- 每次记录生成一个果实，挂到随机的枝头
- 果实颜色 = 情绪分值渐变（红→橙→黄→绿→蓝）
- 果实大小 = 笔记字数
- 点击果实 → 弹层显示当时的记录
- 长满一定数量后树会"开花升级"

**页面**：`pages/mood-tree/mood-tree`

**视觉示意**：
```
       🌳
      /  \
   🌟  🌿  🍃
   /  |  \
 🍎 🍊 🍋  ← 心情果实（颜色映射情绪分）
```

**加分点**：
- 完全自绘 SVG，不依赖图片
- 每个用户的树独一无二
- 月底可以分享"我的情绪树"
- 实现"成长感"的情感化设计

### 8. 徽章成就系统 🏆（提升用户黏性）
**徽章设计建议**（基于现有数据自动解锁）：

| 徽章 | 解锁条件 | 图标 |
|---|---|---|
| 初心 | 记录第 1 条 | 🌱 |
| 三日连心 | 连续 3 天 | 🔥 |
| 周记录者 | 连续 7 天 | 📅 |
| 月观察家 | 连续 30 天 | 🏆 |
| 多元情绪 | 体验过 10 种心情 | 🎭 |
| 阳光日 | 10 次晴天 ≥ 4 分 | ☀️ |
| 雨后彩虹 | 5 次雨天后第二天 ≥ 4 分 | 🌈 |
| 自我探索 | 写满 30 条笔记 | 📝 |
| 客观分析 | EHI ≥ 80 | 💎 |
| 温柔记录 | 雨天写笔记 ≥ 50 字 × 5 次 | ☔ |

**实现位置**：`pages/badges/badges`

**加分点**：解锁时触发"撒花"动画 + 温柔文案。

### 9. 智能提醒与节奏建议 ⏰
**当前缺失**：没有任何提醒功能。

**建议**：
- 用户授权后，每天 20:00 推送"今天记录了吗？"
- EHI < 60 时推送"休息一下，喝杯水吧"
- 3 天未记录推送"我们很想念你 🌿"
- 周末推送周报

**实现**：微信小程序 `wx.requestSubscribeMessage` + 云函数定时触发。

### 10. 呼吸练习与放松工具 🧘
**场景**：用户记录到焦虑/压力大时，弹出一个"先深呼吸 3 次"的按钮。

**实现**：
- 一个全屏的呼吸引导动画（圆环从大到小再到大）
- 4-7-8 呼吸法（吸 4 秒、屏 7 秒、呼 8 秒）
- 完成后给一句温柔的话 + 返回记录页

**加分点**：体现"情感化设计 + 实用价值"。

---

## 四、技术深度升级（P2 · 阶段冲刺）

### 11. AI 情绪洞察（云函数 + 大模型）
**方案**：调用云函数 `getMoodInsight`，将本周记录摘要发给混元/文心一言，生成"温柔的洞察报告"。

**Prompt 设计**：
```
你是一位温柔的心理咨询师。根据以下用户的本周情绪数据，
写一段 100 字的温柔洞察，不要提及具体分数，用第二人称：

{本周数据摘要}
```

**实现**：
- 新增云函数 `getMoodInsight`
- 在周报底部增加"🤖 AI 洞察"卡片
- 失败时降级到当前 rule-based 总结

**加分点**：体现"AI 应用 + 情感化设计"。

### 12. 情绪预测与预警 📊
**算法**：
- 简单滑动平均 + 趋势外推
- 基于历史 7 天预测未来 3 天情绪趋势
- 如果预测连续低谷，弹窗建议"预约一次放松时间"

**可视化**：在晴雨表页增加一条"预测曲线"（虚线）。

### 13. 数据看板优化
**当前**：趋势图是简单的柱状图。

**升级**：
- 增加"情绪热力图"（按星期×时段聚合）
- 增加"情绪河"（stream graph，可视化多心情流动）
- 增加"对比模式"（本周 vs 上周 双柱并列）
- 使用 ECharts for MP（轻量适配）

### 14. 离线优先架构（Offline-First）
**当前问题**：网络差时整个 app 体验下降。

**方案**：
- 所有写入操作 → 先写本地 + 队列 → 后台异步同步云端
- 读取 → 先本地 → 后台静默刷新
- 用 `wx.setStorageSync` + 同步状态字段

**加分点**：体现"工程能力"。

---

## 五、长期愿景（P3 · 长期规划）

### 15. 共情墙（轻社交）💌
- 用户可匿名分享一段心情 + 一句话
- 其他用户可送"🤗" / "🌱" / "💛" 三种安慰
- 周报增加"本周收到 X 个安慰"
- **隐私第一**：不显示用户名、不显示时间

### 16. 多端同步
- 增加微信 H5 版本（用同一套云函数）
- 增加 PC 端（Electron 打包云函数）

### 17. 智能周报升级
- 自动生成 PDF 周报（每月一次）
- 增加"年度情绪报告"（12 月推送）

---

## 六、立即可落地的 Top 5（投入产出比最高）

如果时间紧迫，先做这 5 个：

| # | 功能 | 难度 | 创新分 | 工作量 |
|---|---|---|---|---|
| 1 | 情绪树成长系统 | 中 | ★★★★★ | 3 天 |
| 2 | 徽章成就系统 | 低 | ★★★★ | 2 天 |
| 3 | 智能提醒（推送） | 中 | ★★★ | 1 天 |
| 4 | 呼吸练习 | 低 | ★★★★ | 1 天 |
| 5 | `enhance: true` + 云函数 AI 洞察 | 中 | ★★★★★ | 2 天 |

**做完这 5 个**，综合设计评审基本就是头部水平。

---

## 七、综合设计高分清单

评审老师通常看这 4 个维度：

| 维度 | 当前 | 目标 | 关键动作 |
|---|---|---|---|
| **视觉一致性** | ★★★★★ | ★★★★★ | 已达标 |
| **交互流畅度** | ★★★★ | ★★★★★ | 错误边界、骨架屏 |
| **创新性** | ★★★ | ★★★★★ | 情绪树 + AI 洞察 |
| **情感化设计** | ★★★★ | ★★★★★ | 徽章 + 呼吸 + 温柔文案 |
| **技术深度** | ★★★ | ★★★★★ | enhance:true + 云函数 AI + 离线优先 |
| **完整性** | ★★★★★ | ★★★★★ | 已达标 |
| **实用性** | ★★★★ | ★★★★★ | 数据导出 + 提醒 |

---

## 八、技术债清单

| 文件 | 问题 | 建议 |
|---|---|---|
| `app.js` | `weatherApiKey` 空字段未用 | 删除 |
| `utils/store.js` | 云同步错误吞掉 | 加 UI 提示 |
| `pages/profile/profile.js` | 分享卡代码未被调用（被替换为 navigateTo） | 删除或重构为独立的"分享"入口 |
| `cloudfunctions/getWeather/index.js` | 地图 Key 硬编码 | 移到环境变量 |
| `project.config.json` | `enhance: false` | 改为 `true`（最高优先级）|

---

## 九、我的推荐优先级

**如果你只有 1 周时间**：
1. 改 `enhance: true`（2 小时 + 兼容性回归测试）
2. 情绪树（3 天，全新页面 + SVG 自绘）
3. 徽章系统（2 天，复用现有数据）

**如果你只有 1 天时间**：
1. 加呼吸练习（最容易出彩，1 天搞定）
2. 加每日提醒（半天）
3. 数据导出按钮（半天）

---

需要我直接帮你实现哪个？建议从「情绪树」或「徽章系统」开始 —— 这两个最能体现创新性，且复用现有数据模型。